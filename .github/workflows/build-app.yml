name: Build Desktop App

on:
  push:
    branches: [main, master]
    tags:
      - 'v*'
  pull_request:
    branches: [main, master]

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        node-version: [22.x]

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npm run type-check

      - name: Build frontend
        run: npm run build:frontend

      - name: Build backend JavaScript
        run: npm run build:backend:js

      - name: Build Electron main process
        run: npm run build:electron

      - name: Build backend executable (Windows)
        if: runner.os == 'Windows'
        run: npx pkg dist/backend/index.js --compress Brotli --targets node18-win-x64 --output dist-backend/backend.exe

      - name: Build backend executable (macOS)
        if: runner.os == 'macOS'
        run: npx pkg dist/backend/index.js --compress Brotli --targets node18-macos-x64,node18-macos-arm64 --output dist-backend/backend

      - name: Build backend executable (Linux)
        if: runner.os == 'Linux'
        run: npx pkg dist/backend/index.js --compress Brotli --targets node18-linux-x64 --output dist-backend/backend

      - name: Build desktop app (Windows)
        if: runner.os == 'Windows'
        run: npm run dist:win

      - name: Build desktop app (macOS)
        if: runner.os == 'macOS'
        run: npm run dist:mac

      - name: Build desktop app (Linux)
        if: runner.os == 'Linux'
        run: npm run dist:linux

      - name: Upload artifacts (Windows)
        if: runner.os == 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: news-to-me-windows
          path: dist-final/
          retention-days: 30

      - name: Upload artifacts (macOS)
        if: runner.os == 'macOS'
        uses: actions/upload-artifact@v4
        with:
          name: news-to-me-macos
          path: dist-final/
          retention-days: 30

      - name: Upload artifacts (Linux)
        if: runner.os == 'Linux'
        uses: actions/upload-artifact@v4
        with:
          name: news-to-me-linux
          path: dist-final/
          retention-days: 30

  create-release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            news-to-me-windows/**
            news-to-me-macos/**
            news-to-me-linux/**
          draft: false
          prerelease: false
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
